using Core;

namespace Core
{
    using System;

    public partial struct MaterialreflectTertiarymodule
    {
        public static MaterialreflectTertiarymodule FunctionOne(MaterialreflectTertiarymodule module)
        {
            var array = FunctionTen.FunctionDefaultContainerSurface<Tuple<Materialquery, SecondarymoduleFunctionSeventyPort>>(module.Materialreflect.SecondarymoduleFunctionEightyPortArray);

            var function = new FunctionTen(array);

            FunctionFirst first;

            first = new FunctionFirst(function);

            module.First = first;

            return module;
        }
    }
}
