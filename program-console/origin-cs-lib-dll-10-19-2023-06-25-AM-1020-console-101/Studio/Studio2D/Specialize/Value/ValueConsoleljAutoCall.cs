using Core;

namespace Core
{
    using System;

    public partial class SpecializeConsoleLJ
    {
        public static void ValueConsoleLJAutoCall(Tuple<String, Object>[] array__NAME_VALUE)
        {
            foreach (String stringItem in Specialized.GetAutoCommandStringBuilder())
            {
                /*String stringEntry;

                stringEntry = ConsoleLJ.SafeString(stringItem);

                Boolean hasLengthCheck;

                hasLengthCheck = (stringEntry.Length > 0) is true;

                var lower = stringEntry.ToLower();

                var split = stringEntry.Split();

                ConsoleLJ.EndPointHandler(split, lower, split.Length, hasLengthCheck, array__NAME_VALUE);*/

                continue;
            }

            return;
        }
    }
}
