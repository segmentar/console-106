using Core;

namespace Core
{
    using System;

    internal partial class Program
    {
        internal static void SetupConsoleTreeThird(String[] array_SPLIT, String item_LOWER, Int32 item__SPLIT_LENGTH, Boolean answer_LENGTH_has)
        {
            //var array = ConsoleLJ.EndPointHandler(array_SPLIT, item_LOWER, item__SPLIT_LENGTH, answer_LENGTH_has, ForgeNameValueTuple());

            //ConsoleLJ.CallHandler(array);

            //DuplexHandler(array_SPLIT);

            return;
        }
    }
}
